# 识图能力（vision）

底层模型不具备原生识图能力。遇到图片时，**不要尝试直接读图**，改用 `vision.js`：

```
node vision.js "<图片路径>" "用中文详细描述这张图片"
```

本地图片用文件路径；网络图片用 `node vision.js --url "<图片链接>" "<问题>"`。

## 触发场景

- 用户分享本地图片路径（例如 D:/.../xxx.png）
- 消息中出现 "Saved attachments:" 并列出图片
- 用户要求分析、描述、识别、对比图片内容
- 用户粘贴截图后询问图片内容

## 配置

- API Key / 模型 / 服务地址在项目根目录 `.env` 中配置（见 `.env.example`）
- 运行前需要 `npm install dotenv`（本项目已安装）
- 若识图失败，把 `vision.js` 输出的错误信息原样反馈给用户